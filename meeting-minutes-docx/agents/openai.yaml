interface:
  display_name: "Meeting Minutes DOCX"
  short_description: "整理转写与PPT并生成纪要，支持Marp切分"
  default_prompt: "Use $meeting-minutes-docx to clean transcript + PPT markdown, render and slice Marp decks by report date when needed, and export the final meeting minutes as a .docx."
